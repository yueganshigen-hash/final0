server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gamestore?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: Asis1500055@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  jackson:
    time-zone: Asia/Shanghai
    date-format: yyyy-MM-dd HH:mm:ss

  mail:
    host: smtp.qq.com
    port: 587
    username: 1187328981@qq.com
    password: tburkyggcpyijief
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  secret: GameStore2024VeryLongSecretKeyForJWTSigningPurpose
  expiration: 86400000

logging:
  level:
    com.gamestore: DEBUG

email:
  code:
    expire: 300